import FormIndexer from "../../components/FormIndexer";
import { RegisterContainer } from "./style";

const Register = () => {
  return (
    <RegisterContainer>
      <FormIndexer edit={undefined} />
    </RegisterContainer>
  );
};
export default Register;
